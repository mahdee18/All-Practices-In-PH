// Showing All Elements of an Array
var names = ['Shuvo', 'kobir', 'rimon', 'sara',]
for (var i = 0; i < names.length; i++) {
    var name = names[i]
    console.log(name)
}

// Showing Specific Elemnets of an array
var names = ['rahim', 'karim', 'sarim', 'marim', 'darim', 'shuvo']
for (var i = 0; i < 3; i++) {
    var name = names[i]
    console.log(name)
}
